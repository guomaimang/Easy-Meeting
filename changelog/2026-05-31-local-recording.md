# 2026-05-31 本地录音会话

## 变更

- 新增会议记录模型和本地应用目录管理。
- 新增会议存储服务，开始会议时创建会议目录和 `metadata.json`。
- 新增音频录音器，使用选中的麦克风写入 `audio.m4a`。
- 新增 SQLite 初始化，创建会议和转录分段表。
- 开始/停止录音时同步写入 `meetings` 表。
- 菜单栏新增开始/停止录音入口。
- POC 文档补充本地会议目录验证标准。

## 备注

- 当前还没有历史列表 UI。
- 下一步接入转录分段写入和火山实时语音事件。
