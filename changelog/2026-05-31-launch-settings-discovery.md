# 2026-05-31 启动设置入口

## 变更

- 菜单栏入口从 `EM` 改为 `Easy Meeting`，提高可发现性。
- 菜单顶部增加“设置”入口。
- 配置不完整或 helper 不可用时，启动后自动打开设置窗口。
- 悬浮窗提示用户从顶部菜单栏或设置窗口完成配置。

## 验证

- `swift build` 通过。
- `zsh scripts/package-app.sh` 通过。
